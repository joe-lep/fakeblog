import { RouteData } from '../types';

export const HOME_ROUTE : RouteData = {
  path: '/',
  label: 'Home',
};

export const MY_PROFILE_ROUTE : RouteData = {
  path: '/profile',
  label: 'My Profile',
};
